[project]
name = "rest-rpc-docs"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydoc-markdown>=4.8.2",
    "rest-rpc>=0.1.0",
]

[tool.uv.sources]
rest-rpc = { workspace = true }
